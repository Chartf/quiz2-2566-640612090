export const Comment = ({
  userImagePath,
  username,
  commentText,
  likeNum,
  replies,
}) => {
  const Image = () =>{
    
  }

  const like = () =>{

  }

  return (
    <div>
      
    </div>
  );
};
